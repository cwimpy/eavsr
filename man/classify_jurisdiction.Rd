% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_jurisdiction}
\alias{classify_jurisdiction}
\title{Classify EAVS jurisdictions and derive county FIPS}
\usage{
classify_jurisdiction(df, ct_lookup = NULL, wi_lookup = NULL)
}
\arguments{
\item{df}{An EAVS data frame.}

\item{ct_lookup}{Optional override for the Connecticut town-to-region
table. Defaults to the package's \code{ct_planning_regions} data.}

\item{wi_lookup}{Optional override for the Wisconsin sub-county lookup.
Defaults to the package's \code{wi_subcounty_fips} data; if that data is
unavailable, WI sub-county rows will receive \code{NA} county FIPS.}
}
\value{
The input data frame with the four columns above added.
}
\description{
EAVS reporting units vary across states: some states report at the
county level, others at the sub-county (township/municipality) level,
and a few report multi-county or state-wide rows. This helper adds the
columns needed to roll any EAVS file up to a consistent county-level
geography.
}
\details{
Columns added to \code{df}:
\describe{
\item{\code{is_multi_county}}{Logical; jurisdiction covers multiple counties.}
\item{\code{jurisdiction_type}}{One of \code{"State"}, \code{"Multi-county"},
\code{"County"}, \code{"Sub-county"}.}
\item{\code{state_fips}}{Two-character state FIPS extracted from \code{fips_code}.}
\item{\code{county_fips}}{Five-character county-equivalent FIPS, with
Connecticut planning regions and Wisconsin sub-county lookups
applied where possible. \code{NA} for state and multi-county rows.}
}

Input expectations: the data frame should already have lower-cased
column names (as returned by \code{read_eavs(level = "jurisdiction")} or
\code{janitor::clean_names()}), including \code{fips_code}, \code{jurisdiction_name},
and \code{state_abbr}.
}
\examples{
\dontrun{
  eavs <- read_eavs(2024)
  eavs <- classify_jurisdiction(eavs)
}
}
