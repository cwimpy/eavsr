% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate_to_county}
\alias{aggregate_to_county}
\title{Aggregate EAVS to county level}
\usage{
aggregate_to_county(
  df,
  mode_cols = "d8",
  numeric_fn = function(x) sum(x, na.rm = TRUE),
  drop_territories = TRUE,
  drop_alaska = TRUE
)
}
\arguments{
\item{df}{An EAVS data frame. If it has not already been classified,
\code{classify_jurisdiction()} will be called on it.}

\item{mode_cols}{Character vector of column names that should be
summarized by modal value instead of sum. Defaults to \code{"d8"}.}

\item{numeric_fn}{Function applied to numeric columns during sub-county
aggregation. Defaults to \code{sum(x, na.rm = TRUE)}.}

\item{drop_territories}{Logical. If \code{TRUE} (default), drop AS, GU, MP,
PR, VI from the sub-county aggregation step. Direct-county rows for
these are also dropped.}

\item{drop_alaska}{Logical. If \code{TRUE} (default), drop AK from
sub-county aggregation. AK reports at the borough level, which does
not roll up cleanly to county FIPS.}
}
\value{
A tibble with one row per county-equivalent FIPS code, plus an
added \code{data_source} column (\code{"direct_county"} or \code{"aggregated_subcounty"}).
}
\description{
Combines EAVS rows reported directly at the county level with sub-county
rows summed up to their parent county-equivalent geography. State-level
and multi-county rows are dropped. Optionally drops U.S. territories
and Alaska, which lack standard county-equivalent reporting.
}
\details{
For each county that has a direct EAVS row, that row is preferred over
the aggregated sub-county sum. Sub-county aggregation uses \code{sum(..., na.rm = TRUE)}
for numeric columns by default; specific columns can use a different
summary via \code{mode_cols} (modal value, useful for ordinal questions like
EAVS D8) or \code{numeric_fn} (override the default summarizer).
}
\examples{
\dontrun{
  eavs <- read_eavs(2024)
  county <- aggregate_to_county(eavs)
}
}
