% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_eavs}
\alias{read_eavs}
\title{Read an EAVS cycle as a tidy tibble}
\usage{
read_eavs(
  cycle,
  level = c("jurisdiction", "raw"),
  clean_names = NULL,
  recode_missing = NULL
)
}
\arguments{
\item{cycle}{Integer year of the EAVS cycle.}

\item{level}{Aggregation level: \code{"jurisdiction"} (default, one row per
reporting jurisdiction as released by EAC) or \code{"raw"} (the file
exactly as released, with no recoding).}

\item{clean_names}{Logical. If \code{TRUE} (the default for \code{level = "jurisdiction"}), apply \code{janitor::clean_names()} to column names.}

\item{recode_missing}{Logical. If \code{TRUE} (the default), recode negative
numeric values to \code{NA}. EAVS uses negative codes (\code{-99}, \code{-77}, etc.)
to indicate non-response and missing data.}
}
\value{
A tibble.
}
\description{
Reads a cached EAVS public-release file into R as a tibble, with EAVS
missing-value codes (negative integers) recoded to \code{NA}. Downloads the
file first if it is not already cached.
}
\examples{
\dontrun{
  eavs <- read_eavs(2024)
}
}
