% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_eavs}
\alias{download_eavs}
\title{Download an EAVS cycle}
\usage{
download_eavs(
  cycle,
  what = c("both", "data", "codebook"),
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{cycle}{Integer year of the EAVS cycle (e.g., \code{2024}).}

\item{what}{Which file(s) to download: \code{"data"}, \code{"codebook"}, or
\code{"both"} (the default).}

\item{overwrite}{Logical. If \code{TRUE}, re-download even if a cached copy
exists. Defaults to \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppress progress messages.}
}
\value{
A character vector of local file paths (named by \code{what}).
}
\description{
Downloads the public-release data file (and optionally the codebook) for
a given EAVS cycle from the U.S. Election Assistance Commission and
caches the file locally. Subsequent calls return the cached path without
re-downloading unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
  path <- download_eavs(2024, what = "data")
}
}
